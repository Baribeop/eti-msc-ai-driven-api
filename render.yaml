services:
  - type: web
    name: ai-polyglot-router
    env: python

    buildCommand: |
      pip install -r requirements.txt
      python app/ai/train_model.py

    startCommand: |
      uvicorn app.main:app --host 0.0.0.0 --port 10000